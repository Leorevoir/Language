def main() -> int
{
    /*
    this next line has a syntax error <missing semicolon>
    */
    str = "hello, world!"

    /*
    this next line has a syntax error <missing closing parenthesis>
    */
    print(str;
    return 0;
}
