//INFO: working syntax
/**
* `main` is the entry point of the program
*
* `def` is a keyword used to declare a function
* `main` is the name of the function
* `-> int` tells us that the function will return an integer
*/
def main() -> int
{
    print("Hello, World!\n");
    return 0;
}
